# SPEC 7 — Staged Builder Demo Project (ULTRAPLAN P1.1 / SHOWCASE Phase 3)

**Repo:** /opt/opencode-control-surface. Scope: new `e2e/demo/builder-demo-template/` (small real app template) + `e2e/demo/stage-builder-demo.sh` (one-command stage/reset) + `e2e/demo/BUILDER_DEMO.md` + a builder-beat section appended to `SHOWCASE_DEMO_SCRIPT.md`, plus ONLY surgical `/agent-team` + `/builder` page fixes justified by driving the scenario (each documented file:line). The staged repo instance lives at `/opt/provisioned/builder-showcase-demo` — the ONLY path outside the repo this task may write.

## Goal
The last open ULTRAPLAN P1 box: a small real repo with a scripted failing pass → diagnosis → fix → green, resettable via one command, presentable on /builder + /agent-team for a non-technical viewer. Spine Phase 3's story: "runs a pass → validation/doctor catches a real issue → shown as reversible/traced (a feature, not a failure)".

## Facts you must use (verified by the orchestrator)
- Builder machinery is real and live: `builder_projects` (id/name/root/config_json, e.g. `project:/opt/provisioned/gaffrpro`), `builder_workflows` (mode: `once|auto-continue|scheduled|permanent|doctor|plan`), runs → passes (agentic CLI in tmux, `server/builder/runner.ts`) → `builder_validations` (real shell commands from `validation-profile.ts`) → doctor reports; failures already queue reasoner diagnosis (`queueDiagnosis`) and match `reasoner_playbooks` (`validation-failed` → "Surface to operator" exists).
- Build failures already route into the Insights Inbox (`server/insights/scanners/build.ts`) — spine Phase 3 box 3 is done; your scenario must light that up, not rebuild it.
- Provisioned roots are registered via the existing provision path (`server/builder/provision.ts`, `BUILDER_PROVISIONED_ROOTS` system config) or the projects API — use an existing registration mechanism; NEVER widen `isBuilderProjectRootAllowlisted` or any allowlist to make registration pass.
- API surface: `POST /api/builder/workflows` creates, `POST /api/builder/workflows/:id/<action>` starts/controls (see `server/api/router.ts:820-880`). Auth = operator token → session cookie (`TOKEN=$(grep -E '^OPERATOR_TOKEN=' /etc/control-surface/secrets.env | cut -d= -f2-)`; never echo it).
- Model selection is dynamic (`server/builder/modelSelector.ts`). NEVER hardcode a model id in the template, script, or workflow config — rely on the selector / existing roster resolution.
- The demo runs on the LIVE service under a clearly-labeled project (the bare demo container has no tmux/CLIs). G3 honesty = explicit labeling: project name **"Showcase Builder Demo (staged)"**, workflow name equally explicit.

## Deliverables
1. **`e2e/demo/builder-demo-template/`** — a tiny REAL Bun + TypeScript app (≤5 source files + tests, no framework): e.g. a small utility lib with `bun test` coverage. It must genuinely pass its tests in its fixed state and genuinely fail in its bug state (a real logic bug, not `exit 1` theater). No package installs needed beyond bun built-ins.
2. **`e2e/demo/stage-builder-demo.sh`** — idempotent, one command each way:
   - default (stage): copies the template to `/opt/provisioned/builder-showcase-demo`, `git init` + baseline commit of the GREEN state, then plants the bug as a second commit (so the diff and the revert path are real git objects a presenter can show); registers the builder project + a `once`-mode workflow via existing mechanisms if not already registered; prints exactly what to click next.
   - `--fix`: applies the known-good patch as a real git commit (the deterministic "fix" beat when no capable model is available live).
   - `--reset`: returns the repo to the staged bug state AND cancels/clears this demo's workflow runs (scope deletions strictly to this project/workflow's ids — never a broad DELETE).
3. **Drive the scenario end-to-end at least once** on the live service: start the workflow → pass runs → validation (`bun test` in the demo repo via the validation profile) FAILS on the planted bug → confirm: `builder_validations` failure row, build-failure insight visible in the Insights Inbox, `validation-failed` playbook match recorded, diagnosis queued. Then `--fix` (or a successful agentic pass if the selector lands a capable model — report which happened honestly) → re-run → validation green → run success. Capture evidence (ids, API responses) in BUILDER_DEMO.md.
4. **`/builder` + `/agent-team` narration check**: walk both pages while the scenario is in each state (failing, fixed). Fix ONLY surgical rendering problems (dead link, raw JSON leak, empty-state lie, unclickable control) — each documented file:line. Anything structural: report, don't build.
5. **`e2e/demo/BUILDER_DEMO.md`**: the scenario's presenter guide — stage command, exact clicks, expected screens per state, the honest line between staged data (planted bug, labeled project) and live mechanism (real validation, real insight, real playbook, real git history), plus the evidence you captured. Append a matching "Builder proof beat" section to `SHOWCASE_DEMO_SCRIPT.md` (same honesty format as the existing steps).
6. Verification: `bun run check` clean; full `bun test` 0 fail (946 baseline — add tests for any server code you change); if any `app/` or `server/` file changed, `bash e2e/fresh-host/gate.sh` must exit 0 (zero exceptions) — paste the tail.

## Hard rails
NO git commit/push in /opt/opencode-control-surface (orchestrator commits), NO systemctl, NO pkill. Never touch /opt/newsbites //opt/mimoun //opt/paperclip //opt/backups. `/opt/provisioned/builder-showcase-demo` is the only writable path outside the repo; `git` operations INSIDE that demo repo are allowed and expected. Never widen an allowlist, gate matcher, or validation profile to make anything pass. Leave no workflow running or scheduled when you finish (mode `once`, then verify it's not queued); if the runner orphans a tmux session, kill it by its exact session name only. Live :3000 service and its env stay untouched (you may exercise its HTTP API as an operator — that's the product working). DEMO_SEED never on live. Verify synchronously; no monitor will ever notify you.

## Report back
Scenario evidence chain (workflow/run/pass/validation/insight/playbook ids + API tails), which fix path ran (agentic vs --fix) and why, stage/reset/fix script behavior proof (run each at least once), /builder + /agent-team stumbles + fixes (file:line), changed-file list, test/gate tails.
